/*#include<stdio.h>
#define MAX 1000

int length(char* str) {
	if (*str == '\0') {
		return 0;
	}
	else {
		return 1 + length(str + 1);
	}
}

int main() {
	char str[MAX];

	scanf("%s", str);

	printf("%d", length(str));
}*/
